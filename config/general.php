<?php

return [
    /*
    |--------------------------------------------------------------------------
    | BackendUser image
    |--------------------------------------------------------------------------
    |
    | Set a image url to a fallback image
    |
    */
    'user_fallback_image_url' => '/images/fallback_profile_image.png',

    /*
    |--------------------------------------------------------------------------
    | Disable "auto complete" on email fields
    |--------------------------------------------------------------------------
    |
    | Disables default "auto complete" behaviour on email fields
    | existing on the provided views
    |
    */
    'disable_autocomplete' => false,
];
